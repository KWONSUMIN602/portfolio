import React, { useState } from "react";
import styles from "./BlogStyles.module.scss";

const BlogPresenter = () => {
	return <div className={styles.mainWrapper}>블로그 페이지</div>;
};

export default BlogPresenter;
